[phases.setup]
nixPkgs = ["python311"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[start]
cmd = "mkdir -p ../data/uploads ../data/chroma && uvicorn app.main:app --host 0.0.0.0 --port $PORT"